package booking;


import java.util.Arrays;
import java.util.List;
import java.util.Random;

public class HotelDemo {
    public static Guest createGuest(String firstName, String lastName, String passportNumber){
        Guest guest = new Guest();
        guest.setFirstName(firstName);
        guest.setLastName(lastName);
        guest.setPassportNumber(passportNumber);

        String guest = "P- "+ String.format(%04d, new Random().nextInt(1000000));
        guest.setPassportNumber;

        Random().nextInt()
    }
    public static void main(String[] args) {
        List<String> guestNames = Arrays.asList( "Alice Johnson", "Bob Smith", "Charlie Brown", "Diana Prince",
                "Eve Adams", "Frank Castle", "Grace Kelly", "Henry Ford",
                "Ivy Taylor", "Jack Daniels"
        );
        List<Guest>guests= guestNames.stream().map(names->)

            }
            }

